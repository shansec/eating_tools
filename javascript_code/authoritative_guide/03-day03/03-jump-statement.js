// break语句：break语句会让解释器跳转到循环末尾或跳转到其他语句。
// continue语句：continue语句会让解释器跳出循环体并返回循环顶部开始新一轮迭代。
// return语句会让解释器从函数调用跳转回调用位置，同时提供调用返回的值。
// yied语句：break语句会让解释器跳转到循环末尾或跳转到其他语句。

// 语句标签
// 语句标签名必须是合法的JavaScript标识符
// mainloop: while(token != null) {
//   continue mainloop;  // 跳到命名循环的下一次迭代
// }

// break 和 continue 可以结合语句标签使用