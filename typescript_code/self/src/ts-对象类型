// 对象类型声明
type myObj = {
  x: number
  y: number
  add: (x: number, y: number) => number
}
const obj: myObj = {
  x: 1,
  y: 2,
  add: function (a: number, b: number): number {
    return a + b;
  }
}

// 属性名的索引类型
type myPro = {
  [index: number]: number
}
const pro: myPro = [1, 2, 3]
